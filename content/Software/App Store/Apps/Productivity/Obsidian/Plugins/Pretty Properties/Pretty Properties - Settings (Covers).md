---
created: 2025-06-22T16:17:13Z
tags:
  - documentation/settings
---
## Covers
#### Enable covers
> [!info] In [[Pretty Properties - data.json|data.json]]
> Only written to `data.json` upon update. If there isn't a `data.json`, it will be created.
> 
> **Default:**
> ```JSON
>   "enableCover": true
> ```
#### Cover property
**Requires `"enableCover": true"`.**
> [!info] In [[Pretty Properties - data.json|data.json]]
> Only written to `data.json` upon update. If there isn't a `data.json`, it will be created.
> 
> **Default:**
> ```JSON
>   "coverProperty": "cover"
> ```
#### Folder with images for covers
**Requires `"enableCover": true"`.**
> [!info] In [[Pretty Properties - data.json|data.json]]
> Only written to `data.json` upon update. If there isn't a `data.json`, it will be created.
> 
> **Default:**
> ```JSON
>   "coversFolder": ""
> ```
#### Add extra cover property
**Requires `"enableCover": true"`.**
> [!info] In [[Pretty Properties - data.json|data.json]]
> Only written to `data.json` upon update. If there isn't a `data.json`, it will be created.
> 
> **Default:**
> ```JSON
>   "extraCoverProperties": []
> ```
#### Show covers in page previews
**Requires `"enableCover": true"`.**
> [!info] In [[Pretty Properties - data.json|data.json]]
> Only written to `data.json` upon update. If there isn't a `data.json`, it will be created.
> 
> **Default:**
> ```JSON
>   "enableCoversInPopover": false
> ```

> [!important] #my-settings 
> I set this to `true`.
#### Hide cover when properties are collapsed
**Requires `"enableCover": true"`.**
> [!info] In [[Pretty Properties - data.json|data.json]]
> Only written to `data.json` upon update. If there isn't a `data.json`, it will be created.
> 
> **Default:**
> ```JSON
>   "hideCoverCollapsed": false
> ```
#### Cover default position
**Requires `"enableCover": true"`.**
- left (*"left"*)
- right (*"right"*)
- top (*"top"*)
- bottom (*"bottom"*)
> [!info] In [[Pretty Properties - data.json|data.json]]
> Only written to `data.json` upon update. If there isn't a `data.json`, it will be created.
> 
> **Default:**
> ```JSON
>   "coverPosition": "left"
> ```

> [!important] #my-settings 
> I changed this to `"right"`.
#### Maximum height for left and right covers
**Requires `"enableCover": true"`.**
> [!info] In [[Pretty Properties - data.json|data.json]]
> Only written to `data.json` upon update. If there isn't a `data.json`, it will be created.
> 
> **Default:**
> ```JSON
>   "coverMaxHeight": 500
> ```
#### Maximum height for top and bottom covers
**Requires `"enableCover": true"`.**
> [!info] In [[Pretty Properties - data.json|data.json]]
> Only written to `data.json` upon update. If there isn't a `data.json`, it will be created.
> 
> **Default:**
> ```JSON
>   "coverMaxHeightTopBottom": 400
> ```
#### Default cover width
**Requires `"enableCover": true"`.**
> [!info] In [[Pretty Properties - data.json|data.json]]
> Only written to `data.json` upon update. If there isn't a `data.json`, it will be created.
> 
> **Default:**
> ```JSON
>   "coverDefaultWidth1": 200
> ```
#### Cover width 2
**Requires `"enableCover": true"`.**
> [!info] In [[Pretty Properties - data.json|data.json]]
> Only written to `data.json` upon update. If there isn't a `data.json`, it will be created.
> 
> **Default:**
> ```JSON
>   "coverDefaultWidth2": 250
> ```
#### Cover width 3
**Requires `"enableCover": true"`.**
> [!info] In [[Pretty Properties - data.json|data.json]]
> Only written to `data.json` upon update. If there isn't a `data.json`, it will be created.
> 
> **Default:**
> ```JSON
>   "coverDefaultWidth3": 300
> ```
#### Vertical cover width
**Requires `"enableCover": true"`.**
> [!info] In [[Pretty Properties - data.json|data.json]]
> Only written to `data.json` upon update. If there isn't a `data.json`, it will be created.
> 
> **Default:**
> ```JSON
>   "coverVerticalWidth": 200
> ```
#### Horizontal cover width
**Requires `"enableCover": true"`.**
> [!info] In [[Pretty Properties - data.json|data.json]]
> Only written to `data.json` upon update. If there isn't a `data.json`, it will be created.
> 
> **Default:**
> ```JSON
>   "coverHorizontalWidth": 300
> ```
#### Square cover width
**Requires `"enableCover": true"`.**
> [!info] In [[Pretty Properties - data.json|data.json]]
> Only written to `data.json` upon update. If there isn't a `data.json`, it will be created.
> 
> **Default:**
> ```JSON
>   "coverSquareWidth": 250
> ```
#### Circle cover width
**Requires `"enableCover": true"`.**
> [!info] In [[Pretty Properties - data.json|data.json]]
> Only written to `data.json` upon update. If there isn't a `data.json`, it will be created.
> 
> **Default:**
> ```JSON
>   "coverCircleWidth": 250
> ```
#### Maximal cover width in page preview
**Requires `"enableCover": true"`.**
> [!info] In [[Pretty Properties - data.json|data.json]]
> Only written to `data.json` upon update. If there isn't a `data.json`, it will be created.
> 
> **Default:**
> ```JSON
>   "coverMaxWidthPopover": 150
> ```
