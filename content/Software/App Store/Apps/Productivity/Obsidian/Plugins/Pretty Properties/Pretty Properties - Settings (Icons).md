---
created: 2025-06-22T16:17:13Z
tags:
  - documentation/settings
---
## Icons
#### Enable icons
> [!info] In [[Pretty Properties - data.json|data.json]]
> Only written to `data.json` upon update. If there isn't a `data.json`, it will be created.
> 
> **Default:**
> ```JSON
>   "enableIcon": true
> ```
#### Place icon in inline title
**Requires `"enableIcon": true`.**
> [!info] In [[Pretty Properties - data.json|data.json]]
> Only written to `data.json` upon update. If there isn't a `data.json`, it will be created.
> 
> **Default:**
> ```JSON
>   "iconInTitle": false
> ```
#### Icon property
**Requires `"enableIcon": true`.**
> [!info] In [[Pretty Properties - data.json|data.json]]
> Only written to `data.json` upon update. If there isn't a `data.json`, it will be created.
> 
> **Default:**
> ```JSON
>   "iconProperty": "icon"
> ```

> [!important] #my-settings
> I set this to `"banner-icon"`.
#### Icons folder
**Requires `"enableIcon": true`.**
> [!info] In [[Pretty Properties - data.json|data.json]]
> Only written to `data.json` upon update. If there isn't a `data.json`, it will be created.
> 
> **Default:**
> ```JSON
>   "iconsFolder": ""
> ```
#### Show icons in page previews
**Requires `"enableIcon": true`.**
> [!info] In [[Pretty Properties - data.json|data.json]]
> Only written to `data.json` upon update. If there isn't a `data.json`, it will be created.
> 
> **Default:**
> ```JSON
>   "enableIconsInPopover": false
> ```
#### Icon size
**Requires `"enableIcon": true`.**
> [!info] In [[Pretty Properties - data.json|data.json]]
> Only written to `data.json` upon update. If there isn't a `data.json`, it will be created.
> 
> **Default:**
> ```JSON
>   "iconSize": 70
> ```
#### Icon size on mobile
**Requires `"enableIcon": true`.**
> [!info] In [[Pretty Properties - data.json|data.json]]
> Only written to `data.json` upon update. If there isn't a `data.json`, it will be created.
> 
> **Default:**
> ```JSON
>   "iconSizeMobile": 70
> ```
#### Icon size in page preview
**Requires `"enableIcon": true`.**
> [!info] In [[Pretty Properties - data.json|data.json]]
> Only written to `data.json` upon update. If there isn't a `data.json`, it will be created.
> 
> **Default:**
> ```JSON
>   "iconSizePopover": 50
> ```
#### Icon size in inline title
**Requires `"enableIcon": true`.**
> [!info] In [[Pretty Properties - data.json|data.json]]
> Only written to `data.json` upon update. If there isn't a `data.json`, it will be created.
> 
> **Default:**
> ```JSON
>   "titleIconSize": 30
> ```
#### Text and emoji icons in inline title match title size
**Requires `"enableIcon": true`.**
> [!info] In [[Pretty Properties - data.json|data.json]]
> Only written to `data.json` upon update. If there isn't a `data.json`, it will be created.
> 
> **Default:**
> ```JSON
>   "titleTextIconMatchTitleSize": true
> ```
#### Icon color for the light theme
**Requires `"enableIcon": true`.**
> [!info] In [[Pretty Properties - data.json|data.json]]
> Only written to `data.json` upon update. If there isn't a `data.json`, it will be created.
> 
> **Default:**
> ```JSON
>   "iconColor": ""
> ```
#### Icon color for the dark theme
**Requires `"enableIcon": true`.**
> [!info] In [[Pretty Properties - data.json|data.json]]
> Only written to `data.json` upon update. If there isn't a `data.json`, it will be created.
> 
> **Default:**
> ```JSON
>   "iconColorDark": ""
> ```
#### Icon background
**Requires `"enableIcon": true`.**
> [!info] In [[Pretty Properties - data.json|data.json]]
> Only written to `data.json` upon update. If there isn't a `data.json`, it will be created.
> 
> **Default:**
> ```JSON
>   "iconBackground": false
> ```
#### Icon left margin
**Requires `"enableIcon": true`.**
Can be positive or negative
> [!info] In [[Pretty Properties - data.json|data.json]]
> Only written to `data.json` upon update. If there isn't a `data.json`, it will be created.
> 
> **Default:**
> ```JSON
>   "iconLeftMargin": 0
> ```
#### Icon top margin without banner
**Requires `"enableIcon": true`.**
Can be positive or negative
> [!info] In [[Pretty Properties - data.json|data.json]]
> Only written to `data.json` upon update. If there isn't a `data.json`, it will be created.
> 
> **Default:**
> ```JSON
>   "iconTopMarginWithoutBanner": -10
> ```
#### Icon top margin with banner
**Requires `"enableIcon": true`.**
Can be positive or negative
> [!info] In [[Pretty Properties - data.json|data.json]]
> Only written to `data.json` upon update. If there isn't a `data.json`, it will be created.
> 
> **Default:**
> ```JSON
>   "iconTopMargin": 70
> ```
#### Icon top margin with banner on mobile
**Requires `"enableIcon": true`.**
Can be positive or negative
> [!info] In [[Pretty Properties - data.json|data.json]]
> Only written to `data.json` upon update. If there isn't a `data.json`, it will be created.
> 
> **Default:**
> ```JSON
>   "iconTopMarginMobile": 44
> ```
#### Gap after icon without banner
**Requires `"enableIcon": true`.**
Can be positive or negative
> [!info] In [[Pretty Properties - data.json|data.json]]
> Only written to `data.json` upon update. If there isn't a `data.json`, it will be created.
> 
> **Default:**
> ```JSON
>   "iconGap": 10
> ```
