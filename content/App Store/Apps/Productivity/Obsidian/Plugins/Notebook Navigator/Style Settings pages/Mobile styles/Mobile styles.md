---
summary: Customize mobile interface elements.
tags:
  - style-settings/platform_mobile
---
# Mobile styles
Customize mobile interface elements.
### Mobile pane background
Background color of navigation and list panes on mobile.

> [!info] In `plugins/obsidian-style-settings/data.json`
> Only written to `data.json` upon update. If there isn't a `data.json`, it will be created.
> 
> Only values set through the color picker are accepted; manually editing the JSON file will have no effect.
> 
> **Default:**
> ```JSON
>   "notebook-navigator-style-settings@@nn-theme-mobile-bg@@light": "#",
>   "notebook-navigator-style-settings@@nn-theme-mobile-bg@@dark": "#" 
> ```
> If `⟲` is clicked, the JSON property will be removed from `data.json`.
### Mobile header link color
Color for back button and clickable breadcrumb segments on mobile.

> [!info] In `plugins/obsidian-style-settings/data.json`
> Only written to `data.json` upon update. If there isn't a `data.json`, it will be created.
> 
> Only values set through the color picker are accepted; manually editing the JSON file will have no effect.
> 
> **Default:**
> ```JSON
>   "notebook-navigator-style-settings@@nn-theme-mobile-list-header-link-color@@light": "#",
>   "notebook-navigator-style-settings@@nn-theme-mobile-list-header-link-color@@dark": "#"
> ```
> If `⟲` is clicked, the JSON property will be removed from `data.json`.
### Mobile header breadcrumb color
Color for current folder and separators in breadcrumb on mobile.

> [!info] In `plugins/obsidian-style-settings/data.json`
> Only written to `data.json` upon update. If there isn't a `data.json`, it will be created.
> 
> Only values set through the color picker are accepted; manually editing the JSON file will have no effect.
> 
> **Default:**
> ```JSON
>   "notebook-navigator-style-settings@@nn-theme-mobile-list-header-breadcrumb-color@@light": "#",
>   "notebook-navigator-style-settings@@nn-theme-mobile-list-header-breadcrumb-color@@dark": "#"
> ```
> If `⟲` is clicked, the JSON property will be removed from `data.json`.
### Mobile breadcrumb font weight
Font weight for mobile header breadcrumb.

> [!info] In `plugins/obsidian-style-settings/data.json`
> Only written to `data.json` upon update. If there isn't a `data.json`, it will be created.
> 
> The slider ranges from 100 to 900. Only values set through the slider are accepted; manually editing the JSON file will have no effect.
> 
> **Default:**
> ```JSON
>   "notebook-navigator-style-settings@@nn-theme-mobile-list-header-breadcrumb-font-weight": 600
> ```
> If `⟲` is clicked once, the JSON property is reset to the default value.
> If `⟲` is clicked a second time, the JSON property will be removed from `data.json`.
### Mobile toolbar background
Background color of the mobile toolbar.

> [!info] In `plugins/obsidian-style-settings/data.json`
> Only written to `data.json` upon update. If there isn't a `data.json`, it will be created.
> 
> Only values set through the color picker are accepted; manually editing the JSON file will have no effect.
> 
> **Default:**
> ```JSON
>   "notebook-navigator-style-settings@@nn-theme-mobile-toolbar-bg@@light": "#",
>   "notebook-navigator-style-settings@@nn-theme-mobile-toolbar-bg@@dark": "#"
> ```
> If `⟲` is clicked, the JSON property will be removed from `data.json`.
### iOS glass toolbar background
Background color of the Obsidian 1.11+ iOS glass toolbar (used for border and background, supports transparency).

> [!info] In `plugins/obsidian-style-settings/data.json`
> Only written to `data.json` upon update. If there isn't a `data.json`, it will be created.
> 
> Only values set through the color picker are accepted; manually editing the JSON file will have no effect.
> 
> **Default:**
> ```JSON
>   "notebook-navigator-style-settings@@nn-theme-mobile-glass-toolbar-bg@@light": "#",
>   "notebook-navigator-style-settings@@nn-theme-mobile-glass-toolbar-bg@@dark": "#" 
> ```
> If `⟲` is clicked, the JSON property will be removed from `data.json`.
### Toolbar button icon color
Icon color in mobile toolbar.

> [!info] In `plugins/obsidian-style-settings/data.json`
> Only written to `data.json` upon update. If there isn't a `data.json`, it will be created.
> 
> Only values set through the color picker are accepted; manually editing the JSON file will have no effect.
> 
> **Default:**
> ```JSON
>   "notebook-navigator-style-settings@@nn-theme-mobile-toolbar-button-icon-color@@light": "#",
>   "notebook-navigator-style-settings@@nn-theme-mobile-toolbar-button-icon-color@@dark": "#" 
> ```
> If `⟲` is clicked, the JSON property will be removed from `data.json`.
### Toolbar button active background
Background color for active toolbar button.

> [!info] In `plugins/obsidian-style-settings/data.json`
> Only written to `data.json` upon update. If there isn't a `data.json`, it will be created.
> 
> Only values set through the color picker are accepted; manually editing the JSON file will have no effect.
> 
> **Default:**
> ```JSON
>   "notebook-navigator-style-settings@@nn-theme-mobile-toolbar-button-active-bg@@light": "#",
>   "notebook-navigator-style-settings@@nn-theme-mobile-toolbar-button-active-bg@@dark": "#"
> ```
> If `⟲` is clicked, the JSON property will be removed from `data.json`.
### Toolbar button active icon color
Icon color for active toolbar button.

> [!info] In `plugins/obsidian-style-settings/data.json`
> Only written to `data.json` upon update. If there isn't a `data.json`, it will be created.
> 
> Only values set through the color picker are accepted; manually editing the JSON file will have no effect.
> 
> **Default:**
> ```JSON
>   "notebook-navigator-style-settings@@nn-theme-mobile-toolbar-button-active-icon-color@@light": "#",
>   "notebook-navigator-style-settings@@nn-theme-mobile-toolbar-button-active-icon-color@@dark": "#"
> ```
> If `⟲` is clicked, the JSON property will be removed from `data.json`.

